package com.jinroon.jobe.domain.user.enums;

public class UserEnums {

    public enum UserRole {
        member, admin
    }

    public enum LoginType {
        email, google
    }

    public enum UserStatus {
        active, withdrawn, suspended
    }
}
